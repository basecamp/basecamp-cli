---
name: bcq-only
version: "0.1.0"
description: Basecamp via bcq CLI (no domain hints)
tools:
  - Bash
---

# Basecamp via bcq

Use the `bcq` CLI for all Basecamp operations.

Run `bcq --help` for available commands.
Run `bcq <command> --help` for command-specific help.

Do not call the Basecamp API directly.
